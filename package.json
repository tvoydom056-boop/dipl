{
  "name": "diplom",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/kiks",
    "demo"
  ],
  "scripts": {
    "install:all": "npm ci",
    "build": "npm run build -w kiks && npm run build -w kiks-demo",
    "test": "npm test -w kiks && npm run test:benchmarks -w kiks-demo",
    "typecheck": "tsc -p packages/kiks/tsconfig.json --noEmit && tsc -p demo/tsconfig.json --noEmit && tsc -p benchmarks/tsconfig.json --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "bench": "npm run bench -w kiks-demo",
    "verify": "npm run format:check && npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^25.6.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "eslint": "^10.2.1",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.5.0",
    "jsdom": "^29.0.2",
    "prettier": "^3.8.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.59.0",
    "vitest": "^4.1.5"
  }
}
